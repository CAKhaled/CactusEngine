# توثيق لغة البرمجة KH

مرحباً بك في توثيق لغة البرمجة **KH**، وهي لغة برمجة عربية مبسطة مخصصة لإنشاء الألعاب والتطبيقات التفاعلية بسهولة وسرعة، مع دعم كامل للرسوميات ثنائية وثلاثية الأبعاد (2D & 3D)، الفيزياء، والمدخلات (الماوس ولوحة المفاتيح).

---

## 1. أساسيات اللغة والتهيئة

تعتمد لغة KH على كتابة الأوامر باللغة العربية، وتستخدم المسافات البادئة (Indentation) لترتيب الأكواد كما في لغة بايثون.
لا توجد حاجة لكتابة فواصل منقوطة في نهاية الأسطر.

### تهيئة النافذة
للبدء بإنشاء أي مشروع، يجب أولاً إنشاء نافذة العرض باستخدام الأمر `نافذة`:
```kh
نافذة("اسم اللعبة", 800, 600)
```
يأخذ الأمر ثلاثة مدخلات:
- العنوان (نص بين علامتي تنصيص)
- العرض (بالبكسل)
- الارتفاع (بالبكسل)

### القيم المنطقية (Booleans)
تستخدم لغة KH الكلمات العربية للتعبير عن القيم المنطقية:
- `مفعل` : تعني صحيح (True).
- `مقفل` : تعني خاطئ (False).

### المتغيرات
تعريف المتغيرات بسيط جداً ولا يحتاج إلى كلمات مفتاحية مسبقة، فقط اسم المتغير وقيمته:
```kh
الجاذبية = 0.4
السرعة = 5.0
هل_اللاعب_حي = مفعل
الاسم = "بطل اللعبة"
```

### معرفة نوع المتغير
إذا أردت معرفة نوع البيانات المخزنة داخل متغير، يمكنك استخدام دالة `نوع()`. هذه الدالة ترجع نصاً يمثل النوع (`"رقم صحيح"`, `"رقم عشري"`, `"نص"`, `"مصفوفة"`, إلخ)، مما يسمح لك باستخدامه في الشروط:
```kh
العمر = 25
اطبع نوع(العمر) # سيطبع "رقم صحيح"

النص = "مرحبا"
اطبع نوع(النص) # سيطبع "نص"
```

## الدوال (Functions)
توفر لغة KH إمكانية تعريف دوال مخصصة لتنظيم الكود وإعادة استخدامه. لتعريف دالة، استخدم الكلمة المفتاحية `دالة` متبوعة باسم الدالة والمعاملات بين أقواس. لكي تعيد الدالة قيمة معينة، استخدم الكلمة المفتاحية `ارجع`.

```kh
دالة جمع_رقمين(الرقم_الأول, الرقم_الثاني):
    النتيجة = الرقم_الأول + الرقم_الثاني
    ارجع النتيجة

# استدعاء الدالة
المجموع = جمع_رقمين(10, 5)
اطبع المجموع # سيطبع 15
```

---

## 2. الرياضيات والأنواع الأساسية

توفر اللغة أنواع بيانات جاهزة للتعامل مع الفضاء ثنائي وثلاثي الأبعاد والألوان:

### الأرقام العشوائية
يمكنك توليد رقم عشري عشوائي بين قيمتين (حد أدنى وحد أعلى) باستخدام دالة `عشوائي`:
```kh
سرعة_العدو = عشوائي(2.0, 5.5)
اطبع سرعة_العدو # سيطبع رقماً عشوائياً بين 2.0 و 5.5
```

### المتجهات (Vectors)
لتحديد المواقع أو الأحجام أو الدوران، نستخدم المتجهات:
- **متجه ثنائي (2D)**: للمحورين (س، ص) أو (x, y)
  ```kh
  متجه2(100, 300)
  ```
- **متجه ثلاثي (3D)**: للمحاور (س، ص، ع) أو (x, y, z)
  ```kh
  متجه3(0, 5, -10)
  ```

### الألوان (Colors)
لتحديد الألوان، نستخدم الأمر `لون` ويمثل درجات (أحمر، أخضر، أزرق) تتراوح بين `0.0` و `1.0`:
```kh
اللون_الأحمر = لون(1.0, 0.0, 0.0)
اللون_الأخضر = لون(0.0, 1.0, 0.0)
```

### المصفوفات (Arrays)
تدعم اللغة استخدام المصفوفات لتخزين أكثر من قيمة في متغير واحد واسترجاعها باستخدام الأقواس المعقوفة `[]`:
```kh
الأرقام = [10, 20, 30]
اطبع الأرقام[0] # سيقوم بطباعة 10

الأرقام[1] = 50 # تغيير القيمة في المؤشر 1
```

### القواميس (Dictionaries)
يتيح لك "القاموس" تخزين البيانات على شكل مفاتيح وقيم. وتستطيع تعريف القاموس واستخدامه بهذه الطريقة:
```kh
بيانات = {"الاسم": "خالد", "العمر": 25}
اطبع بيانات["الاسم"] # سيقوم بطباعة خالد

# كما يمكنك استخدام الفاصلة بدلاً من النقطتين (كما في طلبك)
نقاط = {"خالد", 100, "أحمد", 80}
اطبع نقاط["أحمد"] # سيقوم بطباعة 80
```

### البيانات المتداخلة (Nested Data)
تدعم اللغة وضع مصفوفة داخل مصفوفة، أو قاموس داخل مصفوفة (والعكس)، ويمكنك الوصول إليها بشكل متداخل (بدون حد للعمق):
```kh
# مصفوفة بداخلها مصفوفات وقواميس
البيانات = [ [1, 2, 3], {"الاسم": "خالد"} ]

# الوصول إلى المصفوفة الداخلية
اطبع البيانات[0][1] # سيقوم بطباعة 2

# الوصول إلى القاموس الداخلي
اطبع البيانات[1]["الاسم"] # سيقوم بطباعة خالد
```

---

## 3. الرسوميات والأشكال (Graphics & Objects)

تدعم لغة KH إنشاء أشكال ثنائية وثلاثية الأبعاد بسهولة تامة. كل كائن يتم إنشاؤه له خصائص ثابتة يمكنك تعديلها مثل `.الموقع`، `.المقياس`، `.الدوران`، و `.اللون`.

### الأشكال ثنائية الأبعاد (2D)
تتوفر الدوال التالية لإنشاء أشكال 2D:
- `صنع_مربع2D()`
- `صنع_دائرة2D()`
- `صنع_مثلث2D()`
- `صنع_مخصص2D("مسار_الصورة")`

مثال:
```kh
اللاعب = صنع_مربع2D()
اللاعب.الموقع = متجه2(100, 300)
اللاعب.المقياس = متجه2(40, 40)
اللاعب.اللون = لون(1.0, 0.8, 0.0)
```

### الأشكال ثلاثية الأبعاد (3D)
تتوفر الدوال التالية لإنشاء مجسمات 3D:
- `صنع_مكعب()`
- `صنع_كرة()`
- `صنع_سطح()`
- `صنع_هرم()`
- `صنع_اسطوانة()`
- `صنع_كبسولة()`
- `صنع_مخصص("مسار_المجسم")`

مثال:
```kh
المكعب = صنع_مكعب()
المكعب.الموقع = متجه3(0, 0, -5)
المكعب.الدوران = متجه3(0, 45, 0)
المكعب.المقياس = متجه3(2, 2, 2)
```

### خصائص الكائنات
بمجرد إنشاء الكائن، يمكنك الوصول إلى خصائصه وتعديلها في أي وقت:
- `الكائن.الموقع`: مكان الكائن في الشاشة أو الفضاء (متجه2 أو متجه3).
- `الكائن.الموقع.x` و `الكائن.الموقع.y`: يمكن تعديل كل محور على حدة.
- `الكائن.المقياس`: حجم الكائن (عرض وطول).
- `الكائن.الدوران`: زاوية دوران الكائن.
- `الكائن.اللون`: لون المجسم أو الشكل.

### الخامات (Textures) والسماء (Skybox)
يمكنك تجميل الأشكال والنافذة باستخدام الصور والألوان:
- **تحميل الخامات**: `تحميل_خامة("صورة.png")`
- **تغيير السماء/الخلفية**: `صنع_سماء("sky.png")` أو باستخدام لون ثابت `صنع_سماء(لون(0.5, 0.8, 1.0))`

### الكاميرا (Camera)
لا يوجد دالة لإنشاء كاميرا، لأن المحرك يحتوي على كاميرا رئيسية جاهزة للاستخدام تسمى `الكاميرا`. يمكنك التحكم بموقعها وزاوية رؤيتها مباشرة:
```kh
# تحريك الكاميرا في العالم الثلاثي الأبعاد
الكاميرا.الموقع = متجه3(0, 5, 10)

# توجيه الكاميرا (دوران الرأس)
الكاميرا.الدوران.y += 1.5 # الدوران لليمين أو اليسار
الكاميرا.الدوران.x -= 0.5 # النظر للأعلى أو الأسفل
```

### الإضاءة (Lighting)
لإضافة إضاءة للمشهد ثلاثي الأبعاد، يمكنك استخدام دالة `صنع_ضوء()`:
```kh
الضوء = صنع_ضوء()
الضوء.النوع = "ضوء_موجه" # أنواع أخرى: "ضوء_نقطي"، "ضوء_بقعي"
الضوء.الموقع = متجه3(5, 5, 5)
الضوء.اللون = لون(1.0, 1.0, 1.0)
الضوء.الشدة = 1.0
```

### الصوت (Sound)
لتشغيل مؤثرات صوتية أو موسيقى، استخدم دالة `صنع_صوت()`:
```kh
صوت_القفز = صنع_صوت("jump.wav")
صوت_القفز.تشغيل = مفعل # لتشغيل الصوت
# صوت_القفز.تشغيل = مقفل # لإيقاف الصوت
```

---

## 4. حلقة التحديث والشروط (Game Loop & Logic)

تتطلب كل لعبة أو تطبيق "حلقة" تستمر في العمل لتحديث الإطارات باستمرار.

### حلقة الإطارات (كل_فريم)
لعمل حلقة تتكرر باستمرار مع رسم الإطارات (Game Loop)، نستخدم الكلمة المفتاحية `كل_فريم:` (ويجب ترك مسافة بادئة للأسطر التي تحتها).
```kh
كل_فريم:
    المكعب.الموقع.x += 1
```

### الحلقات التكرارية (كرر)
إذا كنت تريد تكرار كود برمجي عدة مرات (مثل حلقات while في اللغات الأخرى) خارج إطار اللعبة، يمكنك استخدام الأمر `كرر:`. 
هذه الحلقة لا تتوقف من تلقاء نفسها، لذلك يجب استخدام أمر `توقف` (Break) عندما يتحقق شرط معين للخروج منها:
```kh
العداد = 0
كرر:
    العداد += 1
    اطبع العداد
    اذا العداد == 5:
        توقف

### الانتظار والمؤقت (مؤقت)
إذا أردت إيقاف تسلسل الكود مؤقتاً لمدة محددة من الزمن (بالثواني)، يمكنك استخدام الأمر `مؤقت(الثواني)`. هذه الدالة مصممة لتعمل بسلاسة حتى لو كان هنالك نافذة رسوميات مفتوحة (حيث تستمر النافذة في العمل ولن تتجمد الشاشة).
```kh
مؤقت(2.5) # الانتظار لمدة ثانيتين ونصف
اطبع "انتهى الانتظار!"
```

### الشروط (اذا) والروابط المنطقية
للتحقق من الشروط نستخدم الكلمة `اذا`:
```kh
اذا هل_اللاعب_حي == مفعل:
    اللاعب.الموقع.y += السرعة
غير_ذلك:
    اطبع "اللاعب ميت!"
```

يمكن التحقق من الأرقام، القيم المنطقية، ومقارنتها باستخدام (`==`, `>`, `<`, `>=`, `<=`).

**الروابط المنطقية (و / او):**
يمكنك دمج أكثر من شرط في نفس السطر باستخدام أدوات الربط `و` (And) أو `او` (Or):
```kh
اذا العمر > 18 و النقاط == 100:
    اطبع "أنت فائز بالغ!"
    
اذا نوع(النص) == "نص" او هل_اللاعب_حي == مفعل:
    اطبع "أحد الشرطين على الأقل تحقق"
```

---

## 5. المدخلات (الماوس ولوحة المفاتيح)

يمكنك التفاعل مع اللاعب عن طريق الماوس ولوحة المفاتيح باستخدام الكائنات المبنية في اللغة `كيبورد` و `ماوس`.

### لوحة المفاتيح (`كيبورد`)
يمتلك كائن `كيبورد` خصائص بأسماء الأزرار لمعرفة ما إذا كانت مضغوطة (`مفعل`) أم لا (`مقفل`).
أمثلة على الأزرار: `.مسافة`, `.w`, `.يسار`, `.يمين`, `أعلى.`, `.أسفل`
```kh
كل_فريم:
    اذا كيبورد.مسافة == مفعل:
        اللاعب.الموقع.y -= 10
```

### الماوس (`ماوس`)
لمعرفة موقع المؤشر وحالة أزراره:
- `ماوس.أفقي`: موقع الماوس الأفقي.
- `ماوس.عمودي`: موقع الماوس العمودي.
- `ماوس.ضغط_يسار`: هل الزر الأيسر مضغوط؟ (`مفعل` / `مقفل`)
- `ماوس.ضغط_يمين`: هل الزر الأيمن مضغوط؟ (`مفعل` / `مقفل`)
- `ماوس.scroll`: مقدار حركة عجلة التمرير.
- `ماوس.معلق`: لتعليق الماوس داخل النافذة وإخفائه، مفيد لألعاب المنظور الأول (`مفعل` / `مقفل`).

```kh
كل_فريم:
    اذا ماوس.ضغط_يسار == مفعل:
        اللاعب.الموقع.x = ماوس.x
        اللاعب.الموقع.y = ماوس.y
```

---

## 6. الفيزياء والاصطدامات

تمتلك اللغة نظام تصادم داخلي رائع ومبسط. كل كائن (مجسم) يمتلك خاصية للتأكد مما إذا كان يتصادم حالياً مع أي كائن آخر أم لا.

- `الكائن.تصادم`: ترجع `مفعل` إذا كان الكائن يتداخل أو يتصادم مع كائن آخر، وإلا ترجع `مقفل`.

مثال من لعبة Flappy Bird للتحقق من الاصطدام:
```kh
كل_فريم:
    اذا الطير.تصادم == مفعل:
        هل_اللاعب_حي = مقفل
        الطير.اللون = لون(1.0, 0.0, 0.0) # تغيير لونه للأحمر عند الخسارة
```

---

## 7. الأصوات (Audio & Sounds)

تدعم اللغة تشغيل الأصوات والمؤثرات الصوتية في ألعابك:
```kh
صوت_القفز = صنع_صوت("jump.wav")

اذا كيبورد.مسافة == مفعل:
    صوت_القفز.تشغيل()
```

---

## 8. واجهة المستخدم (UI Canvas)

تتيح لك اللغة إنشاء واجهة مستخدم تحتوي على نصوص وأزرار تظهر على الشاشة بغض النظر عن الكاميرا أو الأجسام الثلاثية الأبعاد. 

### إنشاء الواجهة
للبدء، يجب عليك أولاً إنشاء الواجهة باستخدام دالة `صنع_واجهة()` وتخزينها في متغير:
```kh
واجهتي = صنع_واجهة()
```

### إضافة النصوص
يمكنك إضافة نص للواجهة باستخدام أمر `اضافة_نص`:
```kh
نص_النقاط = واجهتي.اضافة_نص("النقاط: 0")
نص_النقاط.اللون = لون(1.0, 1.0, 0.0) # لون أصفر
```

### إضافة الأزرار
بنفس الطريقة يمكنك إضافة أزرار والتفاعل معها باستخدام أمر `اضافة_زر`:
```kh
زر_اللعب = واجهتي.اضافة_زر("ابدأ اللعب")
زر_اللعب.لون_الخلفية = لون(0.1, 0.5, 0.1)

كل_فريم:
    اذا زر_اللعب.مضغوط == مفعل:
        اطبع "تم ضغط الزر!"
```

### محاذاة عناصر الواجهة (Anchors)
يمكنك وضع النص أو الزر في أي مكان على الشاشة عن طريق تحديد المحاذاة العمودية والأفقية والإزاحة الحرة:
- **`عمودي`**: `"فوق"`, `"وسط"`, `"تحت"`
- **`افقي`**: `"يمين"`, `"وسط"`, `"يسار"`
- **`ازاحة`**: `متجه2` (لإزاحة العنصر بكسلات محددة عن نقطة المحاذاة)
- **`حجم_الخط`**: للتحكم بحجم النص (الافتراضي 32).
- **`اسم_الخط`**: اسم خط النظام (مثلاً `"arial"` أو `"tahoma"`)، أو مسار لملف خط بصيغة `.ttf`.

بالنسبة **للزر**، توجد خصائص إضافية:
- **`المقياس`**: `متجه2(عرض، طول)` لتحديد أبعاد الزر بشكل ثابت بدلاً من أن يتمدد تلقائياً. (استخدم 0 للبعد التلقائي).
- **`الخامة`**: مسار لصورة (مثلاً `"button.png"`) لاستخدامها كخلفية للزر بدلاً من اللون الموحد.

مثال لوضع زر في منتصف الشاشة بحجم ثابت وخلفية صورة:
```kh
واجهتي = صنع_واجهة()
زر_الخروج = واجهتي.اضافة_زر("خروج")
زر_الخروج.عمودي = "وسط"
زر_الخروج.افقي = "وسط"
زر_الخروج.حجم_الخط = 50
زر_الخروج.المقياس = متجه2(200, 80)
زر_الخروج.الخامة = "my_button_image.png"
```

مثال لوضع نص في أعلى اليسار مع إزاحة بسيطة:
```kh
نص_اللاعب = واجهتي.اضافة_نص("اللاعب الأول")
نص_اللاعب.عمودي = "فوق"
نص_اللاعب.افقي = "يسار"
نص_اللاعب.ازاحة = متجه2(20, 20)
```

---

## مثال تطبيقي شامل:
هذا مثال بسيط للعبة يقوم فيها اللاعب بتحريك مربع وتفادي الاصطدام:

```kh
نافذة("لعبتي الأولى", 800, 600)

اللاعب = صنع_مربع2D()
اللاعب.الموقع = متجه2(400, 300)
اللاعب.المقياس = متجه2(50, 50)
اللاعب.اللون = لون(0.0, 0.5, 1.0)

العدو = صنع_مربع2D()
العدو.الموقع = متجه2(100, 100)
العدو.المقياس = متجه2(60, 60)
العدو.اللون = لون(1.0, 0.0, 0.0)

السرعة = 5.0

كل_فريم:
    # التحكم باللاعب
    اذا كيبورد.w == مفعل:
        اللاعب.الموقع.y -= السرعة
    اذا كيبورد.s == مفعل:
        اللاعب.الموقع.y += السرعة
    اذا كيبورد.a == مفعل:
        اللاعب.الموقع.x -= السرعة
    اذا كيبورد.d == مفعل:
        اللاعب.الموقع.x += السرعة
        
    # التحقق من الخسارة (التصادم)
    اذا اللاعب.تصادم == مفعل:
        اللاعب.اللون = لون(0.0, 0.0, 0.0)
```
